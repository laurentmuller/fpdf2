{
    "require": {
        "php": "^8.2",
        "vimeo/psalm": "^6.0",
        "psalm/plugin-phpunit": "^0.19"
    },
    "config": {
        "preferred-install": {
            "*": "dist"
        },
        "sort-packages": true
    }
}
